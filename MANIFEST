# What ships inside the .skill bundle, one path per line.
#
# Single source of truth: the release pipeline stages from this list AND verifies the
# extracted bundle against it. Two lists would drift, and the drift would be invisible
# because each one would be internally consistent.
#
# README.md is deliberately absent. It is the GitHub-facing document: it links to
# ../../releases/latest and describes scripts/ and .github/, none of which exist inside an
# installed skill. Shipping it would put a file in the bundle that describes a tree the
# bundle does not contain.

SKILL.md
LICENSE
reference/protocol.md
reference/resuming.md
reference/unattended.md
reference/roles.md
reference/approvals.md
reference/verification.md
reference/floor-mechanism.md
reference/git-annex.md
assets/exchange-README.md
assets/message-template.md
assets/gtd-msg.sh
assets/channel-status.sh
assets/bootstrap-code.md
assets/bootstrap-cowork.md
assets/config-template.md
